# 我的问题：
docs\design\新的架构调整对话9.md 的198行# gpt：后面是回答，另外一句产品原则：
“决策流程必须可收束：每轮最多给 3 个选项、只问一个问题，避免用户在多轮复杂选择中崩溃。”作为“流程设计原则”

# glm：
Read d:\HandDeepResearch_AI\ManuScript\docs\design\新的架构调整对话9.md (from line 199)
Read d:\HandDeepResearch_AI\ManuScript\docs\design\新的架构调整对话9.md
非常好！我已经完整理解了所有六轮的讨论和决策。现在让我整合所有决策，输出完整的架构蓝图 + 产品文档。

🎯 ManuScript 重新设计：完整架构蓝图
基于六轮深入讨论的决策汇总，这是进入详细技术方案前的最终蓝图

📋 全部决策汇总（六轮完整版）
第一轮：产品与范围 ✅
决策点	选择	理由
A1 目标用户	硕士生为主（博士深度分析作为后续增强）	硕士需求是"文献整理+引用追溯+写作效率"
A2 论文量级	50-200 篇	不需要复杂向量索引
A3 MVP 边界	A + 对话检索 + 会话摘要 + 笔记（不做写作生成）	平衡"智能交互感"和"可控范围"
A4 成功指标	检索命中率 > 溯源准确性 > 检索速度 > 使用频率	面试 demo 重点是可信度与正确性
第二轮：论文获取与解析 ✅
决策点	选择	理由
B1 获取来源	P0：本地上传 PDF + arXiv 链接下载；P1：arXiv API 搜索	本地为主，外部为辅
B2 解析方案	本地 PDF 解析为主（MVP），arXiv LaTeX 作为 P1 增强	面试 demo 需要可控、隐私友好
B3 解析标准	章节结构 + 段落切分 + 引用标记提取；表格/公式可降级	保证可检索、可溯源
B4 手动粘贴	P1 作为兜底，不纳入 MVP	避免 UI 复杂度
隐私策略	默认本地，云端需用户显式同意；可选脱敏后再云端	强调"本地可控 + 可追溯"
第三轮：存储与索引一致性 ✅
决策点	选择	理由
C1 存储结构	{metadata.json, content.md, notes.md, index.json}	结构清晰、易追溯
C2 段落 ID	方案 C（混合）：正文用位置 ID，笔记用时间戳 ID	兼顾稳定性与可维护性
C3 笔记存储	分文件（content.md + notes.md）	来源区分清晰，索引更稳定
C4 MVP 检索方案	P0：ripgrep 全文检索 + 段落索引	最省工程成本，50-200 篇足够
C5 笔记更新索引	C：只重索引变更段落	性能与一致性平衡
第四轮：检索与溯源体验 ✅
决策点	选择	理由
D1 溯源交互	B：侧边栏展示	不打断搜索流程，信息密度高，demo 观感好
D2 排序优先级	B：匹配度 + 来源优先级（原文/笔记可切换）	核心价值是"原文可追溯"，默认原文优先
D3 来源区分	三重区分（图标 + 标签 + 背景色）	最直观，减少混淆
D4 输入方式	C：支持 A + B（搜索词 + 自然语言，系统自动识别）	兼容习惯，系统自动识别最稳妥
第五轮：多会话与记忆策略 ✅
决策点	选择	理由
E1 共享记忆	三层策略（全局共享 / 会话独立 / 可引用）	清晰分离，避免混乱
E2 压缩触发	B：Token 接近上限 + 用户明确请求 + 会话结束时	覆盖主要场景
E3 冲突处理	B：后写入覆盖 + 修改时间戳显示	平衡简单与可用性
E4 会话数量	B：软限制 3 个，超过后提示用户关闭旧会话	用户体验优先
E5 状态持久化	B：恢复活跃会话，其他会话保留但需手动恢复	平衡复杂度
第六轮：性能与成本 + 隐私与风险 ✅
决策点	选择	理由
F1 性能目标	解析 <10s/篇，检索 <2s，溯源 <0.5s	明确可衡量的性能基准
F2 Token 显示	B：显示当前会话消耗	让用户感知成本
F3 异步任务	B：解析异步，其它同步	平衡性能与复杂度
F4 隐私模式	B：本地模式 + 云端开关	用户可选择，面试强调本地
F5 数据备份	B：提供导出功能	用户可控
🏗️ 最终架构蓝图

┌─────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                              ManuScript v3.0 架构蓝图                                           │
│                              (对话式文献研究助手 - MVP版)                                          │
├─────────────────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                                     │
│  ┌───────────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  Layer 1: 交互层 (UI)                                                          │  │
│  │  ┌─────────────────────┬─────────────────────────────────────────────────────────────────┐  │  │
│  │  │  会话列表（左侧）     │  主对话区域                                              │  │  │
│  │  │  ┌───────────────┐   │  ┌─────────────────────┬─────────────────────────────────┐  │  │  │
│  │  │  │ 会话 1        │   │  │ 对话区              │ 侧边栏（溯源展示）            │  │  │  │
│  │  │  │ 会话 2        │   │  │                     │ ┌───────────────────────────┐ │  │  │  │
│  │  │  │ 会话 3        │   │  │ 输入框              │ │ 原文内容                 │ │  │  │  │
│  │  │  │ [+ 新建会话]  │   │  │ [搜索词/自然语言] │ │ 高亮匹配段落             │ │  │  │  │
│  │  │  └───────────────┘   │  │                     │ │ [笔记] [引用] [复制]    │ │  │  │  │
│  │  │  最多3个，软限制      │  └─────────────────────┴─────────────────────────────────┘  │  │  │
│  │  └─────────────────────┴─────────────────────────────────────────────────────────────────┘  │  │
│  │  显示：Token 消耗 | 当前模式（本地/云端） | 进度反馈                                            │  │
│  └───────────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                              ↓                                                  │
│  ┌───────────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  Layer 2: 路由层                                           │  │
│  │  ┌─────────────────────────────────────────────────────────────────────────────────────────┐  │  │
│  │  │ 意图识别: 闲聊 | 全文检索 | 笔记检索 | 会话摘要 | 导出                                  │  │  │
│  │  └─────────────────────────────────────────────────────────────────────────────────────────┘  │  │
│  │                              ↓                                                         │  │
│  │  ┌──────────────┬──────────────┬──────────────┬──────────────┬──────────────┐           │  │
│  │  │ 闲聊分支     │ 检索分支     │ 笔记分支     │ 摘要分支     │ 导出分支     │           │  │
│  │  │ (小模型/本地)│ (ripgrep)    │ (ripgrep)    │ (LLM)        │ (打包下载)   │           │  │
│  │  └──────────────┴──────────────┴──────────────┴──────────────┴──────────────┘           │  │
│  └───────────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                              ↓                                                  │
│  ┌───────────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  Layer 3: 主 Agent (Main Agent) - 有状态、有记忆、协调者                                    │  │
│  │  ┌─────────────────────────────────────────────────────────────────────────────────────────┐  │  │
│  │  │ 对话状态管理                   │  │  │
│  │  │ • 当前会话状态、用户在哪个阶段、当前正在处理的任务                                      │  │  │
│  │  └─────────────────────────────────────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────────────────────────────────────┐  │  │
│  │  │ 意图理解与任务规划               │  │  │
│  │  │ • 用户想做什么？需要调用哪些工具？                                                    │  │  │
│  │  └─────────────────────────────────────────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────────────────────────────────────────┐  │  │
│  │  │ 长期记忆管理                   │  │  │
│  │  │ • 用户偏好、项目状态、跨会话摘要（可供其他会话引用）                                    │  │  │
│  │  └─────────────────────────────────────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                              ↓                                                  │
│  ┌───────────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  Layer 4: 工具层 (即生即死、无状态)                                                        │  │
│  │  ┌──────────────┬──────────────┬──────────────┬──────────────┬──────────────┐             │  │
│  │  │ Query改写    │ 全文检索     │ 摘要生成     │ 格式转换     │ 脱敏处理     │             │  │
│  │  │ (可选)       │ (ripgrep)    │ (LLM)        │ (Hook)       │ (Hook)       │             │  │
│  │  └──────────────┴──────────────┴──────────────┴──────────────┴──────────────┘             │  │
│  └───────────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                              ↓                                                  │
│  ┌───────────────────────────────────────────────────────────────────────────────────────────────┐  │
│  │  Layer 5: 数据层                     │  │
│  │  ┌─────────────────────────────────────────────────────────────────────────────────────────┐  │  │
│  │  │ 论文存储: papers/{paper_id}/{metadata.json, content.md, notes.md, index.json}           │  │  │
│  │  │                                                                                          │  │  │
│  │  │ 检索: ripgrep 全文检索 + 段落索引                                                        │  │  │
│  │  │                                                                                          │  │  │
│  │  │ 记忆: sessions/*, summaries/*, project_state.json, user_preferences.json                 │  │  │
│  │  └─────────────────────────────────────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                                     │
└─────────────────────────────────────────────────────────────────────────────────────────────────────┘
📄 MVP 产品声明（面试 Demo 版）
ManuScript v3.0

定位: 对话式文献研究助手（硕士研究生版）

目标: 证明"文献可追溯检索 + 笔记检索"的可信闭环

核心流程: 本地导入 PDF → 解析为 Markdown → 全文检索 → 段落级溯源

交互能力: 对话式检索 + 会话摘要 + 笔记管理

成功标准: 检索命中率高、溯源准确、可解释性强

暂不覆盖: 端到端写作生成、博士级深度分析、全量外部搜索

核心卖点: 本地可控 + 可追溯原文 + 笔记入检索

🛠️ 产品设计原则
决策流程可收束: 每轮最多 3 个选项、只问核心问题
可信度优先: 检索命中率 > 溯源准确性 > 检索速度 > 使用频率
本地优先: 默认本地处理，云端需用户显式同意
渐进增强: MVP 先做核心功能，高级功能作为 P1/P2 展示规划
永不丢数据: 降级策略确保用户数据安全
📌 下一步：技术方案与开发计划
现在所有决策已确认，你是否希望我继续输出：

详细技术方案（包括技术栈选型、模块设计、接口定义）
开发计划（包括任务分解、时间估算、依赖关系）
数据结构设计（包括 JSON Schema、数据库设计）
API 接口设计（包括后端接口、前端组件）
请告诉我你希望先输出哪个部分，或者全部输出？

# gpt生成：
# 产品文档（当前阶段定稿）
## 产品定位
- 产品名称：ManuScript v3.0（MVP Demo）
- 目标用户：硕士生为主（博士深度分析为后续增强）
- 典型规模：50–200 篇论文
- 核心价值：本地可控 + 可追溯原文 + 笔记可检索

## MVP 目标与边界
- 目标：证明“文献可追溯检索 + 笔记检索”的可信闭环
- 核心流程：本地导入 PDF → 解析为 Markdown → 全文检索 → 段落级溯源
- 交互能力：对话式检索 + 会话摘要 + 笔记管理
- 不覆盖：端到端写作生成、博士级深度分析、全量外部搜索

## 关键决策摘要
- 获取来源：P0 本地上传 PDF + 手动 arXiv 链接；P1 再做 arXiv/OpenAlex 搜索
- 解析方案：本地 PDF 解析为主（MVP）；arXiv LaTeX 为 P1 增强
- 解析标准：章节结构 + 段落切分 + 引用标记；表格/公式允许降级
- 存储结构：{metadata.json, content.md, notes.md, index.json}
- 段落 ID：混合方案（正文位置 ID + 笔记时间戳 ID）
- 检索方案：P0 ripgrep 全文检索 + 段落索引；P1 SQLite FTS5；P2 向量索引
- 溯源交互：侧边栏展示
- 排序策略：匹配度 + 来源优先级（原文优先，可切换）
- 输入方式：搜索词 + 自然语言双支持

## 多会话与记忆
- 共享策略：全局共享 / 会话独立 / 可引用
- 压缩触发：Token 接近上限 + 用户请求 + 会话结束
- 冲突处理：后写覆盖 + 修改时间戳显示
- 会话限制：软限制 3 个
- 持久化：恢复活跃会话，其它手动恢复

## 性能与成本
- 目标：解析 <10s/篇，检索 <2s，溯源 <0.5s
- Token 展示：显示当前会话消耗
- 异步：解析异步，其它同步

## 隐私与降级
- 默认本地处理，云端需用户显式同意
- 可选脱敏后上云（P1）
- 降级策略：解析失败保留纯文本；LLM 不可用切换本地检索

## 产品原则
- 决策流程可收束：每轮最多 3 个选项，只问一个问题
