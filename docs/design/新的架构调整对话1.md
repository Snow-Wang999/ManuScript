# 我的问题：
我就这3个问题：第一个是死板：我认为第一个就是最初的大纲生成的死板，我的想法是错误的，我认为最开始不是先生成大纲的，也不是按照章节来进行生成，我明确了用户最初的需求是对手里的材料了解不够，论文又多又长，还是英文，记笔记也容易遗忘，需要写作的时候想不起用哪篇文章的哪个观点，这些最重要，那么最初的设计本身就需要考虑到用户的搜索需求是多样化的，而不是固定死的路程。并且用户有通过聊天的方式来增进对目前论文写作的理解与改进，产生灵感，那么论文写作就不是僵化的东西，而是一个渐进的流程，那么自然就不能使用最初的方式进行。所以架构中最初的输入就是灵活的，路由也可能区分是否涉及外部搜索，还是从当前的论文库中搜索，以及如果涉及一些日常对话的需求，我们需要区别专业程度。以及query改写时，这里可能涉及到把日常用语转换为关键字，去用arxiv，openalex等api工具，或者直接进行本地论文库的rag检索。我还考虑到一个问题，用户可能在多轮对话之后才能有清晰的认知，那么单轮对话的设计就过于死板了。除此之外，你提到的A流程层面，确实固定链条很难灵活调整，而且因为上下文无法共享，导致还需要传递上下文，再加上整体的时间消耗过大，对agent本身的能力不太信任，认为他们无法使用复杂的能力。之前的v2.0已经在考虑自主执行的问题，但我不确定是否还是有过度设计。所以v1.0的架构也许是需要跳过的。B性能：执行太慢，是否有可并行的流程。C交互层面：我确实认为用户无法中途干预，一直是个问题，首先流程不是很透明，不知道干到哪儿了，不知道等多久，还有不知道花了多少时间等，这些让用户难以忍受。D：扩展层面，我希望能够更加灵活一些，我可能在一定程度上想部分接入我上一个项目方向的论文搜索与下载全文的能力。我可能还希望加入一个本地论文数据脱敏的一个功能。考虑到隐私问题，可能会涉及如果用户希望本地处理它的论文，我希望脱敏后再到云端处理，降低泄密风险等。第二个是架构：我认为目前的架构是有问题的，我还涉及一些决策问题。我清楚的是论文搜索如果是偏理科的，我可以获取latex原文，如果已有pdf，也可以让用户选择获取latex，这样会更容易获取表格与公式。而如果是文科就是走pdf解析的道路。而如果我获取不到，就让用户自己获取论文。问题1：本地论文库是以什么形式存在，一个方法我可能希望我解析后的论文内容放到一个markdown文档，这样我能用类似bash，glob或正则表达式的方法轻易搜索到像claude code，其中markdown可以在笔记：的后面记录他的笔记，我认为这种方式简单，另一个方法就是向量数据库，但是我们依然希望用户可以直接查看论文原文的内容，以及我们生成的内容的来源，追溯到某一个段落与原文。问题2：我可能并不清楚架构的划分，比如哪些是放到主agent里，哪些是子agent，哪些是mcp的工具，哪些是hook，哪些是记忆，毕竟用户写论文有一个周期的。第三个是subagent：关于主agent和subagent之间其实上下文是隔离的，subagent是只有短期记忆的，他们的生命周期很短，我们不希望它执行特别复杂的任务，他是即生即死的，做完任务就回收的性质，你说的多个section的并行生成，我是希望他像会话一样，可以同时开三个窗口，进行对话的，但最多只能并行3个，这样可能更合理一些，多个 Query 并行生成？
多个数据源的并行检索？
多个 Chunk 的并行验证？这几个也许可以实现并行操作。你先看看，我们讨论看看

# GLM的回答：
非常感谢你详细的回答！你的思考非常清晰，很多痛点已经看得非常准了。让我整理一下你的核心观点，然后我们来逐一深入讨论。

📌 你的核心观点提炼
痛点总结
维度	你的观察	根本原因
设计假设错误	一开始就生成大纲 → 按章节生成	误解了用户真实需求：用户首先是对材料不熟悉，需要渐进式理解，不是一开始就要写完整论文
流程僵化	固定6步链	无法应对多样化搜索需求、无法支持多轮对话式探索
交互不透明	用户不知道进度、等待时间、成本	缺少进度反馈和干预机制
上下文传递重	Agent间强耦合	过度分解，本该是内部步骤的拆成了独立Agent
扩展困难	新增功能成本高	架构分层不清晰
你提出的正确方向
输入应该灵活：不是固定大纲，而是支持多样化搜索、聊天式探索
多轮对话：用户需要通过多轮交互才能形成清晰认知
路由层：需要区分外部搜索 vs 本地RAG vs 日常对话
Query改写：日常用语 → 关键词/检索式
论文周期记忆：用户写论文有一个周期，需要跨会话记忆
多窗口并行：像ChatGPT一样开多个会话，最多3个
🎯 第一轮讨论：重新定义产品形态
你的核心洞察是：这不是一个"论文生成器"，而是一个"论文研究助手"。

这个定位的改变会彻底改变架构设计。我画一张对比图：


┌─────────────────────────────────────────────────────────────────────┐
│                        错误的定位（v1.0）                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   用户输入题目 → 系统生成大纲 → 按章节生成 → 输出完整论文            │
│       ↓              ↓               ↓              ↓               │
│   一次性输入      固定流程        串行执行      黑盒过程             │
│                                                                     │
│   问题：假设用户已经知道要写什么，只是需要"生成"出来                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                        正确的定位（你的方向）                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   用户 ──多轮对话──→ 系统 ──渐进式理解──→ 用户逐渐清晰               │
│    ↓                    ↓                    ↓                     │
│  "我有50篇论文      帮你搜索、整理、        "现在我知道了          │
│   不知道怎么写"      总结、对比、关联"       该怎么写了"            │
│                                                                     │
│   本质：这是一个"研究工具"，不是"生成器"                             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
问题 1：用户旅程的重新定义
基于你的理解，真实的用户旅程应该是这样的吗？


阶段 1: 探索期
├── 用户：我有个模糊方向（深度学习在医学影像）
├── 系统搜索本地库 → 告诉用户有哪些相关论文
├── 用户：翻看某几篇 → 提问："这些论文主要用了什么方法？"
├── 系统总结：主要用了CNN、Transformer...
├── 用户：继续追问...

阶段 2: 结构化期
├── 用户：经过多轮对话后，逐渐形成大纲
├── 系统：不是"生成大纲"，而是"帮助用户整理思路"
├── 用户：可以随时调整、修改

阶段 3: 写作期
├── 用户：选择某个章节开始写
├── 系统：基于之前的对话上下文，提供素材建议
├── 用户：一边写一边继续问问题

阶段 4: 润色期
├── 用户：检查引用、验证准确性
├── 系统：帮助校验、补充缺失的引用
请确认：这个用户旅程是否符合你的预期？还是你有不同的理解？

🏗️ 第二轮讨论：架构分层决策
你提到了一个很关键的问题：不清楚架构的划分（主agent、subagent、MCP工具、Hook、记忆）。

让我给你一个清晰的分层模型：


┌─────────────────────────────────────────────────────────────────────┐
│                      ManuScript 架构分层                             │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Layer 1: 用户交互层                          │   │
│  │  • 聊天界面（多会话、最多3个并行）                             │   │
│  │  • 进度展示（当前在做什么、预计多久）                           │   │
│  │  • 成本显示（用了多少token）                                   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              ↓                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Layer 2: 路由层                   │   │
│  │  ┌──────────┬──────────┬──────────┬──────────┐              │   │
│  │  │ 闲聊    │ 本地搜索 │ 外部搜索 │ 写作任务 │              │   │
│  │  └──────────┴──────────┴──────────┴──────────┘              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              ↓                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Layer 3: 主 Agent (Main Agent)                              │   │
│  │  • 对话状态管理（多轮上下文）                                  │   │
│  │  • 意图理解与任务规划                                         │   │
│  │  • 调用 Sub-agent/工具                                       │   │
│  │  • 长期记忆（用户的论文周期）                                  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              ↓                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Layer 4: Sub-agent / 工具层                                 │   │
│  │  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │   │
│  │  │ Query生成   │ 本地RAG     │ 外部搜索    │ 内容验证    │  │   │
│  │  │ (Sub-agent) │ (MCP工具)   │ (MCP工具)   │ (Sub-agent) │  │   │
│  │  └─────────────┴─────────────┴─────────────┴─────────────┘  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              ↓                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Layer 5: 数据层                           │   │
│  │  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │   │
│  │  │ 对话历史    │ 论文库      │ 用户笔记    │ 写作状态    │  │   │
│  │  │ (短期记忆)  │ (Markdown) │ (Markdown) │ (状态机)    │  │   │
│  │  └─────────────┴─────────────┴─────────────┴─────────────┘  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
问题 2：这个分层模型是否清晰？
具体到你的问题：

主 Agent：负责对话管理、意图理解、长期记忆
Sub-agent：Query生成、内容验证（即生即死，无状态）
MCP 工具：本地RAG检索、外部API调用
Hook：论文脱敏、格式转换（插入处理流程）
记忆：短期（对话历史）、长期（论文周期状态）
这个划分是否合理？还有什么需要澄清的？

💾 第三轮讨论：本地论文库的存储方式
你提出了两个方案：

方案	优点	缺点
A. Markdown	可直接搜索、可读性强、支持笔记	检索质量不如向量
B. 向量数据库	语义检索准确	不透明、难以追溯原文
我的建议：混合方案

┌─────────────────────────────────────────────────────────────┐
│                    混合存储方案                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  论文 PDF                                                   │
│      ↓                                                      │
│  ┌──────────────────┐                                      │
│  │   解析器         │                                      │
│  │  • LaTeX提取     │ ← 理科优先，获取表格公式               │
│  │  • PDF解析       │ ← 文科走这条路                        │
│  └────────┬─────────┘                                      │
│           ↓                                                 │
│  ┌─────────────────────────────────────────────┐           │
│  │ 论文.md                    │           │
│  │ ┌─────────────────────────────────────────┐ │           │
│  │ │ # 元数据                                  │ │           │
│  │ │ title: xxx                               │ │           │
│  │ │ arxiv_id: xxx                            │ │           │
│  │ └─────────────────────────────────────────┘ │           │
│  │ ┌─────────────────────────────────────────┐ │           │
│  │ │ # 内容（分章节）                          │ │           │
│  │ │ ## Abstract                              │ │           │
│  │ │ 内容...                                  │ │           │
│  │ │ ## 1. Introduction                       │ │           │
│  │ │ 内容...                                  │ │           │
│  │ └─────────────────────────────────────────┘ │           │
│  │ ┌─────────────────────────────────────────┐ │           │
│  │ │ # 笔记（用户可编辑）                      │ │           │
│  │ │ ## Notes:                                │ │           │
│  │ │ - 这篇论文的核心贡献是...                │ │           │
│  │ └─────────────────────────────────────────┘ │           │
│  └─────────────────────────────────────────────┘           │
│           ↓                                                 │
│  ┌──────────────────┐      ┌──────────────────┐            │
│  │ 全文搜索        │      │ 向量检索         │            │
│  │ (grep/glob/正则) │      │ (语义搜索)       │            │
│  └──────────────────┘      └──────────────────┘            │
│           ↓                         ↓                       │
│           └───────────┬─────────────┘                       │
│                       ↓                                       │
│                 检索结果 + 原文定位                           │
│                 (可追溯到具体段落)                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
优势：

Markdown 作为主存储：透明、可编辑、支持笔记
可选的向量索引：用于语义检索，但原文依然在 Markdown
检索结果可追溯到原文的某个段落
问题 3：这个混合方案是否符合你的需求？
🔄 第四轮讨论：多轮对话与并行会话
你提到的"多轮对话"和"多窗口并行"是关键特性：


┌─────────────────────────────────────────────────────────────┐
│                    多会话管理（最多3个）                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  会话 1: "探索深度学习在医学影像的方法"    [活跃]            │
│  会话 2: "整理引言部分的素材"            [暂停]             │
│  会话 3: "验证实验部分的引用"            [暂停]             │
│                                                             │
│  每个会话：                                                 │
│  • 独立的对话历史                                           │
│  • 共享的论文库和用户笔记                                    │
│  • 独立的当前任务状态                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
问题 4：会话间的"共享记忆"应该如何设计？
选项 A：每个会话完全独立，但共享论文库
选项 B：会话之间可以"引用"（比如会话2可以说"基于会话1的讨论..."）
选项 C：全局的"项目状态"（大纲、写作进度）所有会话可见

你倾向哪种？

🚀 问题总结
请回答以下问题，我们继续深入：

用户旅程：上面描述的"探索→结构化→写作→润色"4个阶段是否符合你的预期？

架构分层：5层模型（交互层→路由层→主Agent→Sub-agent/工具→数据层）是否清晰？

论文存储：Markdown为主 + 可选向量索引的混合方案，是否满足你的需求？

会话管理：3个并行会话，你希望它们之间如何共享信息？（A/B/C哪个方案？）

额外问题：你提到"部分接入论文搜索与下载全文"和"本地论文脱敏"，这两个功能优先级如何？是 MVP 必需还是后期扩展？