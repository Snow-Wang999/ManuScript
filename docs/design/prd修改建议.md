好的，你的决策已确认：

| 决策点 | 你的选择 |
|--------|----------|
| MVP检索方式 | **B：MVP就用混合检索** |
| Embedding模型 | **C：本地+API都支持，用户可选** |
| 脱敏处理 | **A：MVP不做，P1做** |

---

基于这些决策，我来帮你整理**PRD需要修改的具体内容**：

## 📝 PRD修改清单

### 修改1：5.1.3 解析方案（第259-263行）

**原文：**
```markdown
| 方案 | 选择 | 说明 |
|------|------|------|
| **MVP** | 本地 PDF 解析 | 使用 pdfplumber/PyMuPDF |
| **P1** | arXiv LaTeX 优先 | 理科论文表格/公式更准确 |
| **兜底** | 纯文本保留 | 确保总能解析 |
```

**改为：**
```markdown
| 方案 | 选择 | 说明 |
|------|------|------|
| **MVP** | Marker + PyMuPDF | Marker处理学术PDF（表格/公式/多栏），PyMuPDF做fallback |
| **P1** | arXiv LaTeX 优先 | pylatexenc解析，保留公式结构 |
| **兜底** | 纯文本 + OCR | Tesseract处理扫描件 |
```

---

### 修改2：8.1 论文存储结构（第660-667行）

**原文：**
```markdown
papers/
├── {paper_id}/
│   ├── metadata.json
│   ├── content.md
│   ├── notes.md
│   └── index.json
```

**改为：**
```markdown
data/
├── papers/
│   └── {paper_id}/
│       ├── metadata.json          # 元数据
│       ├── content.md             # 正文（带段落ID，可grep）
│       ├── notes.md               # 用户笔记
│       └── index.json             # 段落位置索引
│
├── indexes/                        # 全局索引层（新增）
│   ├── fts.db                     # SQLite FTS5 全文索引
│   └── vectors/                   # 向量索引
│       └── chroma/                # ChromaDB 持久化目录
│
└── config/
    └── embedding.json             # Embedding配置（本地/API切换）
```

---

### 修改3：新增 7.4 检索架构（在7.3之后插入）

```markdown
### 7.4 混合检索架构

```
用户查询
    ↓
┌───────────────────────────────────────────────────┐
│              Query Processor                       │
│  意图识别 → 搜索词/自然语言 → Query重写            │
└───────────────────────┬───────────────────────────┘
                        ↓
        ┌───────────────┼───────────────┐
        ↓               ↓               ↓
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  ripgrep    │  │  FTS5       │  │  向量检索   │
│  精确匹配   │  │  倒排索引   │  │  语义相似   │
└──────┬──────┘  └──────┬──────┘  └──────┬──────┘
       └────────────────┼────────────────┘
                        ↓
              RRF 融合排序 (Reciprocal Rank Fusion)
                        ↓
                   Top-K 结果
```

#### 7.4.1 检索策略

| 查询类型 | 触发条件 | 检索方式 |
|----------|----------|----------|
| 精确搜索 | 引号包裹、特定术语 | ripgrep 优先 |
| 模糊搜索 | 普通关键词 | FTS5 + 向量 |
| 语义搜索 | 自然语言问句 | 向量优先 + FTS5辅助 |

#### 7.4.2 Embedding配置

| 模式 | 模型 | 要求 | 适用场景 |
|------|------|------|----------|
| **本地模式** | BGE-M3 / BGE-large-zh | 4GB显存 | 隐私敏感 |
| **API模式** | OpenAI text-embedding-3-small | 需API Key | 无显卡用户 |

用户可在设置中切换，默认检测本地显卡自动选择。
```

---

### 修改4：11.4 技术栈选型（第876-891行）

**在检索部分补充：**

```markdown
| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| **解析** | Marker | 最新 | 学术PDF主解析器 |
| | PyMuPDF | 最新 | Fallback |
| | pylatexenc | 最新 | LaTeX解析（P1） |
| **检索** | ripgrep | 最新 | 精确关键词匹配 |
| | SQLite FTS5 | 3.x | 全文倒排索引 |
| | ChromaDB | 0.4+ | 向量检索 |
| **Embedding** | BGE-M3 | 最新 | 本地模式（多语言） |
| | OpenAI API | - | API模式（可选） |
```

---

### 修改5：11.1 MVP优先级（第842-854行）

**更新P0任务：**

```markdown
| 编号 | 任务 | 验收标准 | 预估工作量 |
|------|------|----------|------------|
| P0-1 | **论文解析 → Markdown** | Marker解析PDF，输出带段落ID的Markdown | 2-3天 |
| P0-2 | **混合检索** | ripgrep + FTS5 + ChromaDB向量检索 | 4-5天 |
| P0-3 | **Embedding双模式** | 本地BGE-M3 / OpenAI API可切换 | 1-2天 |
| P0-4 | **溯源展示（侧边栏）** | 点击跳转准确率 > 95% | 2天 |
| P0-5 | **笔记管理** | 添加/编辑/删除/检索笔记 | 2天 |
| P0-6 | **会话摘要 + 长期记忆** | Token触发 + 用户请求触发 | 2天 |
| P0-7 | **多会话管理** | 最多3个会话，软限制 | 2天 |
| P0-8 | **UI 实现** | Gradio 三栏布局 | 3天 |

**P0 总计**：约 18-21 天
```

---

### 修改6：11.2 P1增强功能

**更新P1任务（加入脱敏）：**

```markdown
| 编号 | 任务 | 依赖 | 预估工作量 |
|------|------|------|------------|
| P1-1 | arXiv API 搜索与下载 | P0-1 | 2天 |
| P1-2 | LaTeX 解析（pylatexenc） | P0-1 | 3-5天 |
| P1-3 | **NER脱敏处理** | P0-2 | 2-3天 |
| P1-4 | Reranker精排 | P0-2 | 2天 |
| P1-5 | 云端模式开关 | P0-8 | 1天 |
```

---

## ✅ 下一步

你希望我：

1. **直接生成修改后的PRD文档**（合并以上所有修改）
2. **先讨论其他设计文档**（数据结构/接口定义/模块设计）是否也需要对齐修改

你选哪个？